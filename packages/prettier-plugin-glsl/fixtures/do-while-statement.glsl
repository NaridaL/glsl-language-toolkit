void main() {
    // empty block
    do{} while (i++ < 4);

    do{
              a = b + c;
          } while (i++ < 4);

    // single statement
    do foo(); while (bar());

    do; while(i-- > 0);
}